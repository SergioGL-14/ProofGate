---
description: Audit a ProofGate contract and its evidence without editing
---

Load the `proofgate` skill and execute the `audit` operation.

Use `$ARGUMENTS` as the user's audit target and optional intensity or profile
selection. Inspect through ADVERSARY without editing, identify weak or absent
contract evidence, and issue the audit verdict with residual risk.
