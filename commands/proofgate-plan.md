---
description: Create a ProofGate contract and evidence design without editing
---

Load the `proofgate` skill and execute the `plan` operation.

Use `$ARGUMENTS` as the user's task and optional intensity/profile selection.
Do not edit files or issue PASS, FAIL, BLOCKED, or EXCEPTION. Stop after TEST
DESIGN and emit the required `PROOFGATE PLAN (NO VERDICT)` artifact.
