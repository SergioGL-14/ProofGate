---
description: Implement a ProofGate task under its authorized contract
---

Load the `proofgate` skill and execute the `build` operation.

Use `$ARGUMENTS` as the user's task and optional intensity/profile selection.
Follow the full lifecycle, edit only within the authorized workspace and
contract, run the relevant gates, attempt adversarial invalidation, and issue
the exact final verdict with executed evidence and residual risk.
